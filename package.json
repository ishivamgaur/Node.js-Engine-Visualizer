{
  "name": "node-engine-visualizer",
  "version": "1.0.0",
  "description": "Visualize Node.js internals - Call Stack, Event Loop, Task Queues, Libuv & more",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "cd client && npm install"
  },
  "devDependencies": {}
}
